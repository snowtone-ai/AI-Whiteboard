# issues.md — current blockers only

No active blocker is recorded.

All three site adapters (chatgpt.com, claude.ai, gemini.google.com) are `verified` as of round 12
(2026-08-21) — each has been confirmed by the owner's own hands with the packaged, unpacked
extension against their real, logged-in session. See `tasks.md` P006/P007/P008 and
`docs/adapter-smoke.md` for the evidence trail.

When a blocker appears, add one row with the task ID, exact symptom, owner, needed decision,
and the next safe action. Remove the row when resolved; this file is read at session start and
is not an archive of old failures.
